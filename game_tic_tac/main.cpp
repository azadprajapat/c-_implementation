#include<bits/stdc++.h>
#include<iostream>
using namespace std;
class TicTacToe{
    vector<vector<int>> board;
    public:
    TicTacToe(int n){ 
        board.resize(n,vector<int>(n));
    }
    int move(int player,int row, int col)
}
int main(){

    return 0;
}